import style from "./Navbar.module.css";

export const Navbar = () => {
    return (
        <header className={style.headerTop}>
            <h1 className={style.title}>Bin2Dec</h1>
        </header>
    )
}